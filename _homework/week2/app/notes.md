# 1 задача
1. пользователь выбирает мероприятие `event_id` и список мест `seat_ids`
2. эндпоинт принимает `event_id`, `seat_ids`, а также `user_id`, создается
    бронь на BOOKING_TTL минут (`reserved_until`) со статусом `pending_payment` (ожидает оплаты)
    2.1 проверять наличие ивента по `event_id` (добавить `get_event_by_id` метод)
    2.2 проверять наличие каждого места по `seat_id` (добавить `get_seat_by_id` метод)
    2.3 проверять соответствие брони пользователю
3. если оплата прошла в течение 15 мин, то создавать запись в `EventSeat` со статусом
    `reserved`
